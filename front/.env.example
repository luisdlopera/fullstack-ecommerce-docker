NEXT_PUBLIC_API_URL=http://localhost:4000/api
# Local `next dev`: use localhost so Route Handlers (/api/bff) can reach Nest. In Docker, override to http://back:4000/api (see docker-compose.yml).
INTERNAL_API_URL=http://localhost:4000/api
